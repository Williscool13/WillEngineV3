# .wenvmap / .wprobe Format Changelist
#
# Magic: "wenvmap" (WEnvMapHeader) / "wprobe" (WProbeHeader)
# Loaders reject major != *_MAJOR_VERSION. Stale .wprobe requires re-bake.

================================================================================
.wprobe VERSION 2.1  (current)
================================================================================
- "radiance_scale" (hex float bits): stored radiance * scale = nits. Bakes rescale every face to the smallest face pre-exposure.

================================================================================
.wprobe VERSION 2.0
================================================================================
- Header snapshot floats now hex bits ("0x%08x").

================================================================================
.wprobe VERSION 1.0
================================================================================
- Pre-changelist. See git history.

================================================================================
.wenvmap VERSION 1.0  (current)
================================================================================
- Pre-changelist. Integer-only header. See git history.
