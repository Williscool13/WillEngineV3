# .wscene Format Changelist
#
# Magic: "wscene". Header: WSceneHeader in scene_format.h
# Loader rejects major != SCENE_MAJOR_VERSION.

================================================================================
VERSION 2.0  (current)
================================================================================
- Body is the bespoke text format (docs/serialization/text_format.md), was JSON.
  entity record blocks, component sub-blocks keyed by decimal typeId, editor_camera block.

================================================================================
VERSION 1.0
================================================================================
- Pre-changelist. Text header + JSON body. See git history.
